<Window x:Class="SysMonitor.Setup.SetupWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="SysMonitor"
        Width="460" Height="360"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        Background="#ffffff">

    <Grid Margin="26,22,26,20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock x:Name="Heading" Grid.Row="0" FontSize="19" FontWeight="SemiBold"
                   Foreground="#0f172a"/>

        <TextBlock x:Name="Subheading" Grid.Row="1" Foreground="#64748b" FontSize="12"
                   TextWrapping="Wrap" Margin="0,6,0,0"/>

        <StackPanel x:Name="Options" Grid.Row="2" Margin="0,18,0,0"/>

        <TextBlock x:Name="Status" Grid.Row="3" Foreground="#64748b" FontSize="12"
                   TextWrapping="Wrap" Margin="0,0,0,12"/>

        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button x:Name="CancelButton" Content="ยกเลิก"/>
            <Button x:Name="ActionButton" Content="ติดตั้ง" Style="{StaticResource Primary}"/>
        </StackPanel>
    </Grid>
</Window>
