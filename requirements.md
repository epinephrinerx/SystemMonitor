1. traffic light guage 70% = เหลือง 90% = แดง
2. ในกรณีที่มี CPU, Ram หรือ Disk หลายๆ ชุด แก้ให้เป็น card ไม่ต้องเรียงจากบนลงล่าง แต่ให้เป็น 2 columns
3. แรมถ้าแยกเป็นแต่ละชุดได้ ให้แยกด้วย
4. เพิ่มหน้าต่างเต็มจอ และเมื่อขยายเต็ม ให้เพิ่มความสามารถในการสร้างกราฟเต็มรูปแบบ สำหรับแต่ละอุปกรณ์
5. เพิ่มการตรวจวัด Lan, WLAN โดยแสดงค่า upload, download
6. หยุดทำ Python Version ให้คงไว้แค่ C# Version

## Full-Screen (2026-09-21)

1. full screen mode หมายถึงหน้าจอใหญ่สุดที่แสดง Graph การทำงานทั้งหมด ไม่ใช่ Windows Full Screen Mode
2. Full Screen Mode ให้แสดงค่าแยกตามกลุ่มของอุปกรณ์ โดยอุปกรณ์ทุกรายการ ให้เรียงจากบนลงล่าง และแยกกลุ่มตามประเภท Design ให้มีกรอบแยกตามประเภท และสร้างเป็น Vertical Tab (แก้จาก Horizontal ตามที่สั่งเพิ่มเติม) เพื่อให้ทั้งหมดแสดงได้ในหน้าเดียว หากต้องการเปลี่ยนกลุ่มของอุปกรณ์ ให้คลิกเลือก Tab แทนการแสดงผลทั้งหมด และให้ขนาดสัมพันธ์กับการย่อขยายขนาดของหน้าต่างได้ โดยหน้าต่างมีขนาดเล็กสุดได้ที่ 640*480 ถ้าเล็กกว่านั้น ให้กลับไปแสดงเป็นหน้าต่างกลาง (Expanded)
3. หน้าเต็ม ให้เพิ่ม icon สำหรับคลิก โดยบอกว่าเป็น Full Data หรือข้อความที่คล้ายๆ กัน ไว้ด้านซ้ายมือ เหนือการตั้งค่าหน้าต่าง
4. เมื่อเข้าไปแล้ว ให้เรียงลำดับ TAB เหมือนหน้ากลางคือ CPU, Memory, Disk โดย Disk ให้แยกแต่ละ Logical Disk ออกเป็นแต่ละ Tab เลย โดยบอกว่า Logical Disk นี้ อยู่ใน Physical Disk ไหน ขนาด Partition เป็นเท่าไรจากเท่าไร ใช้ไปแล้วเท่าไร อุปกรณ์ physical นั้นชื่ออะไร มีคุณสมบัติต่างๆ อย่างไร ให้ได้ข้อมูลที่ละเอียดที่สุด
5. Design ทุกหน้า ให้เริ่มต้นโปรแกรมด้วย light mode พื้นหลังสีขาว และทุกหน้าให้เป็น light mode เป็น default
6. หน้า widget ให้มีปุ่มซ้ายขวา เพื่อเลื่อนไปยัง Item ถัดไป โดยไม่ต้องรอหน้าจอ refresh เอง

## Full-Screen รอบแก้ (2026-09-21)

7. ข้อมูลให้อยู่ตามหลังชื่ออุปกรณ์ใน vertical tab (แบบ Task Manager: ชื่อ → ชนิดฮาร์ดแวร์ → ค่าที่วัดได้)
8. หน้า CPU ยังไม่มีข้อมูล ให้เพิ่มรายละเอียดตัวประมวลผล
9. คำว่า Memory ให้เปลี่ยนเป็น RAM
10. ข้อมูล Drive ซ้ำซ้อน เห็น C: และขนาดอยู่แล้ว ให้เหลือแค่ NTFS, SATA/NVMe, ยี่ห้อ/รุ่น ตัด Serial และ Firmware ออก
[x] 11. Vertical tab ของ CPU: บรรทัดชื่อคงคำว่า CPU ไว้เฉย ๆ ชื่อรุ่น CPU ให้อยู่ที่หัวกรอบด้านขวาเหมือนเดิม (ไม่ย้ายมาที่ rail เพราะชื่อรุ่นของเครื่องอื่นอาจยาวจนดันความกว้าง rail) / บรรทัดถัดมาใช้รูปแบบ 12C/16T ตามด้วยสัญญาณนาฬิกาเป็นตัวเลขล้วน (ตัดคำว่า "ความเร็วพื้นฐาน" ออก) / บรรทัด % ให้พ่วงอุณหภูมิไว้ด้วย ผลคือกรอบข้อมูลด้านขวาเหลือเพียง Virtualization, L2, L3 ไม่มีอย่างอื่น
[x] 12. กรอบด้านขวาของ CPU: การ์ดราย Core ให้วาดเป็นสี่เหลี่ยมจัตุรัส สูงครึ่งหนึ่งของการ์ดผลรวม CPU เรียงต่อกันเป็นแถวและตัด/เพิ่มจำนวนต่อแถวเองเมื่อผู้ใช้ปรับขนาดหน้าต่าง พร้อมแสดงอุณหภูมิ โดยอุณหภูมิเป็นของ CPU รวม ไม่มีค่าราย Core (เซ็นเซอร์ราย Core อยู่หลัง MSR ต้องใช้ kernel driver)
[x] 13. หน้าหลัก (Expanded) โหมดแยกราย Core: ตัดตราอุณหภูมิออกจากแถว C0..Cn เพราะค่าที่ใส่อยู่เป็นค่าของทั้งแพ็กเกจที่ซ้ำกันทุกแถว ไม่ใช่ค่าที่วัดราย core จริง (WidgetViewModel บรรทัด ~451) ส่วนแถวผลรวม CPU ยังคงแสดงอุณหภูมิเหมือนเดิม
[x] 14. ต่อจากข้อ 13: หน้าหลักต้องยังเห็นอุณหภูมิกลางของ CPU เสมอ รวมถึงตอนอยู่ในโหมดแยกราย Core ซึ่งปัจจุบันไม่มีแถวผลรวมเลย (Section มีแค่ Title กับ Rows) — **ผู้ใช้เลือกทางเลือก ก.** คือเติมอุณหภูมิแพ็กเกจไว้ที่หัวข้อ "CPU" ของ section เช่น `CPU · 58°C` ต้องเพิ่ม property ให้ Section และแก้ XAML ของหัวข้อ ไม่กินแถวเพิ่ม (ไม่ใช้ทางเลือก ข. ที่คงแถวผลรวมไว้บนสุด)
[x] 15. หน้าข้อมูลเต็ม แท็บ RAM: บรรทัดข้อมูลบน rail ให้เป็นรูปแบบ `xx GB / yy GB` (เว้นวรรครอบเครื่องหมาย ทับ) ส่วนกรอบรายละเอียดด้านขวาให้เหลือเฉพาะข้อมูลแยกราย module เท่านั้น ตัดแถวขนาดรวมและแถวที่ใช้งานอยู่ออก เพราะซ้ำกับ rail และกราฟด้านล่าง
[x] 16. ต่อจากข้อ 15: หัวกรอบขวาของแท็บ RAM ให้เปลี่ยนจากสรุปโมดูล (`1 × 16 GB DDR4 2667` ซึ่งผิดเมื่อใส่โมดูลไม่เหมือนกัน) เป็นจำนวนสล็อตที่ใช้ เช่น `2 จาก 4 สล็อต` — ต้องอ่านจำนวนสล็อตทั้งหมดเพิ่มจาก `Win32_PhysicalMemoryArray.MemoryDevices` เพราะ `Win32_PhysicalMemory` ที่ใช้อยู่ให้เฉพาะสล็อตที่มีโมดูลเสียบอยู่
[x] 17. หน้าข้อมูลเต็ม แท็บ Disk: rail ให้เป็นชื่อไดรฟ์ตามด้วยชื่อพาร์ทิชัน (เช่น `C: (Windows)`) บรรทัดถัดมาเป็น File system และชนิด (เช่น `NTFS · SSD (NVMe)`) และบรรทัดสุดท้ายเปลี่ยนจาก % ล้วนเป็น `xx GB / yy GB (%)` ส่วนยี่ห้อ/รุ่น คงไว้ที่หัวกรอบด้านขวาเหมือนเดิม ของเดิมแท็บ Disk มีสามส่วนคือ กรอบรายละเอียด การ์ดพื้นที่ใช้งาน และการ์ดอ่าน/เขียน **ให้เหลือเฉพาะการ์ดอ่าน/เขียน** ตัดกรอบรายละเอียดและการ์ดพื้นที่ใช้งานออกทั้งคู่ (กรอบรายละเอียดยุบเองอยู่แล้วเมื่อ Facts ว่าง จึงไม่กระทบแท็บ CPU กับ RAM ที่ยังใช้กรอบนี้) อุณหภูมิไดรฟ์ให้พ่วงท้ายบรรทัดชนิดบน rail เช่น `NTFS · SSD (NVMe) · 41°C` และไดรฟ์ที่ไม่มีเซ็นเซอร์ก็แสดงแค่สองส่วนแรกตามเดิม
[x] 18. คำศัพท์เรียกหน้าต่างสามหน้าให้เป็น widget / overall / full ทั้งในบทสนทนา ในโค้ด และในคีย์ของ config — `View.Mini` → `View.Widget`, `View.Expanded` → `View.Overall`, `MiniW/MiniH` → `WidgetW/WidgetH`, `ExpW/ExpH/ExpSized` → `OverallW/OverallH/OverallSized`, `MinMini` → `MinWidget`, `MinExp` → `MinOverall` คีย์ใน config.wpf.json จะกลายเป็น `widget_w/widget_h/overall_w/overall_h/overall_sized` เองตาม SnakeCaseLower ต้องเขียน migration อ่านคีย์เก่า (`mini_*`, `exp_*`) มาใส่ให้เมื่อคีย์ใหม่ไม่มี ไม่อย่างนั้นขนาดหน้าต่างที่ผู้ใช้ตั้งไว้จะหายเงียบ ๆ ตอนอัปเกรด
[x] 19. ค่าเริ่มต้นของ "เมื่อกดปิด" ให้เป็น "ย่อลง Tray" แทน "ออกจากโปรแกรม" — `AppConfig.CloseAction` เปลี่ยนค่าเริ่มต้นจาก "exit" เป็น "tray" มีผลกับผู้ติดตั้งใหม่เท่านั้น เพราะเครื่องที่มี config อยู่แล้วจะอ่านค่าเดิมจากไฟล์
[x] 20. เพิ่มการแสดงผล GPU ในหน้า overall และ full — แหล่งข้อมูลคือ performance counter ชุดเดียวกับที่ Task Manager ใช้ ผ่าน P/Invoke `pdh.dll` (ไม่ต้องใช้ NuGet ไม่ต้องยกสิทธิ์): `\GPU Engine(*)\Utilization Percentage` จัดกลุ่มตาม engtype แล้วรวมในกลุ่ม เอาค่าสูงสุดข้ามกลุ่มเป็นเลข GPU รวม, `\GPU Adapter Memory(*)\Dedicated Usage` และ `Shared Usage` สำหรับหน่วยความจำ, `Win32_VideoController` สำหรับชื่อรุ่นและเวอร์ชันไดรเวอร์ ตรวจบนเครื่องนี้แล้วใช้ได้จริง (Intel Iris Xe Graphics, ไดรเวอร์ 32.0.101.7079, engtype 3d อ่านได้ 7.2%) ยังไม่ทราบต้นทุน: เครื่องนี้มี 1182 instance ต้องเขียน spike วัดเวลาของ PdhGetFormattedCounterArray ใน C# ก่อน ถ้าแพงเกินจังหวะ metrics (2.5 วินาที) ให้ย้ายไปจังหวะช้าหรือหาทางอื่น อุณหภูมิ GPU ทำไม่ได้บนเครื่องนี้ Task Manager เองก็ขึ้น N/A เพราะเป็น iGPU และค่านั้นอ่านได้เฉพาะการ์ดแยกผ่าน D3DKMTQueryAdapterInfo ซึ่งไม่มีเอกสารรองรับ
[x] 21. หน้า full: รวมแท็บ disk จากรายไดรฟ์เป็นรายดิสก์จริง แบบเดียวกับ Windows Disk Management — rail หนึ่งรายการต่อ Disk 0, Disk 1, ... โดยบอกชนิดดิสก์ ขนาด การเชื่อมต่อ และสถานะ (เช่น `SSD (NVMe) · 232.9 GB · Online`) ตามด้วยยี่ห้อ/รุ่น ส่วนกรอบด้านขวาให้ไล่ไดรฟ์ในดิสก์นั้นเรียงตามตัวอักษร (C:, D:, ...) แต่ละไดรฟ์ระบุชื่อ, File system, Usage `xx GB / yy GB (%)` และอัตราอ่าน/เขียน — บนเครื่องนี้จะยุบจาก 7 แท็บเหลือ 3 (Disk 0 = D:, Disk 1 = C: กับ R:, Disk 2 = E: F: G:) ไดรฟ์ที่ไม่มีดิสก์จริงรองรับ เช่น H: ของ Google Drive ซึ่งไม่มีแถวใน MSFT_Partition เลย ให้รวบไว้เป็นหัวข้อเดียวท้าย rail ชื่อ “ไดรฟ์เสมือน” / “Virtual drives” (key: diskvirtual) บรรทัดสองบอกจำนวนไดรฟ์ บรรทัดสามไล่ตัวอักษร เช่น `H:` (ผู้ใช้ยืนยันชื่อนี้แล้ว) และต้องอ่าน IsOffline หรือ OperationalStatus ของ MSFT_Disk เพิ่ม เพราะตอนนี้ DiskDetails อ่านแค่ HealthStatus
[x] 22. ไม่แสดงพาร์ทิชันที่ไม่ใช่ของ Windows (EFI System, Microsoft Reserved, Recovery, Linux ฯลฯ) เพราะเป็นแถวนิ่งที่ไม่มีอะไรให้ทำต่อ แต่ให้เก็บ "พื้นที่ยังไม่จัดสรร" ไว้ โดยอ่านจาก `MSFT_Disk.AllocatedSize` เทียบกับ `Size` ซึ่งอยู่ใน query เดิมอยู่แล้ว แล้วเติมท้ายบรรทัดชนิดบน rail เฉพาะเมื่อมากกว่าศูนย์ เช่น `SSD (NVMe) · 232.9 GB · Online · 12 GB ว่าง` (เครื่องนี้เป็นศูนย์ทั้งสามดิสก์ จึงจะยังไม่เห็น)
23. ค่าเริ่มต้นสำหรับเครื่องที่ติดตั้งใหม่ — Always on top = ปิด, Snap to edge = เปิด, Start with Windows = เปิด, Light mode = เปิด, ปุ่มปิด = ย่อลง Tray, ภาษา = English, Opacity = 100%, Font size = 100%, และข้อมูลที่แสดงผลเปิดทั้งหมด (CPU, แยกราย Core, RAM, Disk, แยกราย Drive, อ่านอุณหภูมิ CPU จริง, GPU, Network, แยกราย Adapter) ต้องแก้ `AppConfig.AlwaysOnTop` เป็น false, `Lang` เป็น "en", `Opacity` เป็น 1.0 ส่วนที่เหลือเป็นค่าที่ถูกอยู่แล้ว และในตัวติดตั้ง `SetupWindow` ต้องติ๊ก "เปิดพร้อม Windows" ไว้ล่วงหน้า เพราะตอนนี้อ่านจาก `Installer.AutostartEnabled()` ซึ่งเป็น false บนเครื่องที่ยังไม่เคยติดตั้ง
[x] 24. แสดงไดรฟ์เครือข่าย (L:, N:, P:) ซึ่งตอนนี้ถูกกรองทิ้งที่ `Win32.LogicalDrives` เพราะ `Sampler` ไม่ได้ส่ง `includeNetwork` เข้าไป และค่าเริ่มต้นของพารามิเตอร์นั้นเป็น false — เพิ่ม `AppConfig.IncludeNetwork` กับ checkbox ในแผงตั้งค่า แล้วจัดเป็นหัวข้อของตัวเองชื่อ "ไดรฟ์เครือข่าย" / "Network drives" (key: disknetwork) แยกจาก "ไดรฟ์เสมือน" เพราะ GetDriveType แยก DRIVE_REMOTE ออกจาก DRIVE_FIXED ได้ชัดเจนและ This PC ก็เรียกมันว่า Network Locations ต่างหากอยู่แล้ว ต้องระวังว่า share ที่เครื่องปลายทางหลับหรือ VPN หลุดจะบล็อกจน SMB timeout จึงต้องให้ `Guard()` คุมด้วย budget ที่หลวมกว่าไดรฟ์ปกติ
[x] 25. Tray icon ไม่แสดงเพราะสองชั้น — ชั้นแรก config ของผู้ใช้ยังเป็น close_action "exit" ไอคอนจึงไม่เคยถูกสร้าง (กลไกเองทำงานถูก ทดสอบแล้ว log ขึ้น "hidden to tray") ชั้นที่สอง Windows 11 เก็บไอคอนใหม่ทุกตัวไว้หลังลูกศรถาดซ่อน จนกว่าจะมี `IsPromoted=1` ใน `HKCU\Control Panel\NotifyIconSettings\<hash>` ซึ่งรายการของเรามีอยู่แล้วแต่ไม่มีค่านี้ — ให้ตั้ง IsPromoted ให้ตัวเองหนึ่งครั้ง โดยหารายการที่ `ExecutablePath` ตรงกับ exe ของเรา และจำไว้ใน config ว่าเคยตั้งแล้ว เพื่อไม่ให้ไปดันกลับทุกครั้งถ้าผู้ใช้ลากมันกลับเข้าถาดซ่อนเอง และเปลี่ยน close_action ในเครื่องผู้ใช้เป็น tray ให้ด้วย
[x] 26. เพิ่มฟังก์ชั่น update ในตัวโปรแกรม — ตรวจหาเวอร์ชันใหม่จาก GitHub releases เทียบกับเวอร์ชันของตัวเอง ถ้ามีใหม่กว่าให้ดาวน์โหลดตัวติดตั้งแล้วรันต่อได้เลย ใช้ HttpClient ของ BCL ไม่พึ่ง NuGet
[x] 27. หน้าต่างตัวติดตั้ง (SysMonitor.Setup) ให้ใช้ light theme แทนของเดิม
[x] 28. บั๊กที่เจอระหว่างทำข้อ 26 — `Installer.Version` เป็น const เขียนตายตัวไว้ที่ "3.1.0" ไม่ได้ถูกอัปเดตตอนขึ้นเวอร์ชัน 3.2.0 ทำให้ release 3.2.0 บันทึก DisplayVersion ผิดใน Add/Remove Programs และหน้าตัวติดตั้งขึ้นว่าจะแทนที่ 3.2.0.0 ด้วย 3.1.0 — แก้ให้อ่านจาก assembly แทน พร้อมเทสต์บังคับให้เลขของแอปกับของตัวติดตั้งตรงกัน
